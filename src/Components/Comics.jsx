const Comics = () => {
    return (
        <div>
            <h2>Comics</h2>
            <p>This component is still being reviewed to be connected to database. Check back later!</p>
        </div>
    );
};

export default Comics;